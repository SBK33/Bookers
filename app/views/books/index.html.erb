<h1>Books</h1>
<!--テーブルタグで、dbの情報を表示。IDは一番上が若くなる Show Edit Destroyにリンクをつける-->
<h2>New book</h2>
<%= form_with model: @book do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :body %>
  <%= f.text_field :title %>
<% end %>

