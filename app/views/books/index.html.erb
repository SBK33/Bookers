<h1>Books</h1>
<!--テーブルタグで、dbの情報を表示。IDは一番上が若くなる Show Edit Destroyにリンクをつける-->

<h2>New book</h2>
<%= form_with model: @book do |f| %>
  <p>Title</p>
    <%= f.text_field :title %>
  <p>Body</p>
    <%= f.text_area :body %>
  <div>
    <%= f.submit 'Create Book' %>
  </div>
<% end %>

